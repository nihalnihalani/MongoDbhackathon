# Base URL of the RECEIPTS backend.
# The app runs perfectly without it: every request falls back to the bundled
# case file and the header shows a "Fixture" badge.
VITE_API_BASE=http://localhost:3001
